<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>GreenShop — Home</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/public/css/styles.css" rel="stylesheet">
</head>
<body>

  <!-- Header/Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-success">
    <div class="container">
      <a class="navbar-brand fw-bold" href="/home">🌱 GreenShop</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item"><a class="nav-link" href="/products">Products</a></li>
          <li class="nav-item"><a class="nav-link" href="/products/add">Add Product</a></li>
          <li class="nav-item"><a class="nav-link" href="/orders">Orders</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Hero Section -->
  <section class="bg-light text-center py-5">
    <div class="container">
      <h1 class="display-4 fw-bold">Welcome to GreenShop</h1>
      <p class="lead">Your eco-friendly marketplace for sustainable living.</p>
      <a href="/products" class="btn btn-success btn-lg">Browse Products</a>
    </div>
  </section>

  <!-- Product Preview Grid -->
  <section class="container py-5">
    <h2 class="text-center mb-4">Featured Products</h2>
    <div class="row g-4">
      <% if (products && products.length > 0) { %>
        <% products.slice(0,4).forEach(p => { %> <!-- show 4 latest products -->
          <div class="col-md-3">
            <div class="card h-100 shadow-sm">
              <img src="<%= p.imageUrl %>" class="card-img-top" alt="<%= p.name %>">
              <div class="card-body">
                <h5 class="card-title"><%= p.name %></h5>
                <p class="card-text text-muted">£<%= p.price.toFixed(2) %></p>
                <a href="/products" class="btn btn-outline-success btn-sm">View More</a>
              </div>
            </div>
          </div>
        <% }) %>
      <% } else { %>
        <p class="text-center">No products yet. <a href="/products/add">Add one</a>.</p>
      <% } %>
    </div>
  </section>

  <!-- Footer -->
  <footer class="bg-dark text-light py-3 mt-5">
    <div class="container text-center">
      <p class="mb-0">&copy; <%= new Date().getFullYear() %> GreenShop. All rights reserved.</p>
    </div>
  </footer>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
